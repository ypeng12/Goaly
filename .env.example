# Docker Compose reads .env. For local execution, export these variables instead.
# Leave the key empty for the deterministic demo. Never commit a real token.
AI_API_KEY=
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini
PORT=8080
